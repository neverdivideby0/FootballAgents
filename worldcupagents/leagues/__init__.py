"""League packs (MULTILEAGUE_PLAN.md) — one engine, many competitions.

A League descriptor supplies only what differs between competitions (code, stage
rules, venue model). The World Cup is the default pack so existing behaviour is
unchanged; club leagues plug in the same way.
"""
